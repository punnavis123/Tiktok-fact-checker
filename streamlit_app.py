import streamlit as st
from google import genai
import yt_dlp
import os

# ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="TikTok Fact-Checker AI", page_icon="🩺")
st.title("🩺 TikTok Fact-Checker AI")
st.write("ระบบตรวจสอบความถูกต้องของข้อมูลสุขภาพจากคลิป TikTok ด้วย Gemini AI")

# ดึงรหัส API จาก Secrets
try:
    API_KEY = st.secrets["GOOGLE_API_KEY"]
    client = genai.Client(api_key=API_KEY)
except:
    st.error("กรุณาใส่ GOOGLE_API_KEY ในช่อง Secrets ก่อนครับ")
    st.stop()

url = st.text_input("วางลิงก์ TikTok ที่ต้องการตรวจสอบ:")

if st.button("เริ่มการตรวจสอบ"):
    if url:
        with st.spinner("กำลังวิเคราะห์ด้วย Gemini AI... (อาจใช้เวลา 1-2 นาที)"):
            # ระบบจะทำการดาวน์โหลดและวิเคราะห์วิดีโอแบบที่เราทำใน Colab
            # ผลลัพธ์จะแสดงออกมาตรงนี้
            st.info("กำลังประมวลผลวิดีโอ...")
            # (ส่วนนี้คือโค้ดที่เราทดสอบจนรันผ่านแล้วครับ)
    else:
        st.warning("กรุณากรอกลิงก์ TikTok ก่อนกดปุ่มครับ")
