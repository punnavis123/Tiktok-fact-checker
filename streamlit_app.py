import streamlit as st
from google import genai
import yt_dlp
import os
import time

# --- 1. ตั้งค่าหน้าเว็บให้สวยงาม ---
st.set_page_config(page_title="TikTok Fact-Checker AI", page_icon="🩺")
st.title("🩺 TikTok Fact-Checker AI")
st.write("ระบบตรวจสอบข้อมูลสุขภาพด้วยพลัง AI (Gemini 3.0/1.5)")

# --- 2. ดึงกุญแจ API จากระบบ Secrets ---
try:
    API_KEY = st.secrets["GOOGLE_API_KEY"]
    client = genai.Client(api_key=API_KEY)
except:
    st.error("❌ ไม่พบ API Key! กรุณาตรวจสอบในช่อง Secrets ของ Streamlit")
    st.stop()

# --- 3. ส่วนรับลิงก์จากผู้ใช้ ---
url = st.text_input("วางลิงก์ TikTok ที่ต้องการตรวจสอบ:")

if st.button("🚀 เริ่มการตรวจสอบ"):
    if url:
        # ใช้ st.status เพื่อสร้างกล่องสถานะที่ดูเป็นมืออาชีพ
        with st.status("🛠️ กำลังเริ่มกระบวนการ... โปรดรอสักครู่", expanded=True) as status:
            
            # --- ขั้นตอนที่ 1: ดาวน์โหลดวิดีโอ ---
            st.write("📥 กำลังดาวน์โหลดวิดีโอจาก TikTok...")
            ydl_opts = {
                'format': 'best',
                'outtmpl': 'temp_video.mp4',
                'overwrites': True,
                'nocheckcertificate': True # เพิ่มความเสถียรในการโหลดบน Cloud
            }
            
            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([url])
                st.write("✅ ดาวน์โหลดสำเร็จ!")
            except Exception as e:
                st.error(f"ดาวน์โหลดล้มเหลว: {e}")
                st.stop()

            # --- ขั้นตอนที่ 2: อัปโหลดไปที่ Google ---
            st.write("🧠 กำลังส่งข้อมูลให้ AI วิเคราะห์...")
            with open("temp_video.mp4", "rb") as f:
                uploaded_file = client.files.upload(file=f, config={'mime_type': 'video/mp4'})
            
            # รอให้ AI ประมวลผลวิดีโอ
            while uploaded_file.state.name == "PROCESSING":
                time.sleep(3)
                uploaded_file = client.files.get(name=uploaded_file.name)
            st.write("✅ AI รับข้อมูลเรียบร้อย!")

            # --- ขั้นตอนที่ 3: สั่งให้ AI ตรวจสอบ ---
            st.write("⚖️ กำลังตรวจสอบข้อเท็จจริง (Fact-checking)...")
            
            # คำสั่ง Prompt ที่เข้มงวด (Grounding)
            prompt = """
            ในฐานะผู้เชี่ยวชาญทางการแพทย์:
            1. ถอดสคริปต์ภาษาไทยจากวิดีโอนี้
            2. วิเคราะห์ว่าคำแนะนำในคลิปมีความเสี่ยงหรือเป็นข้อมูลเท็จหรือไม่
            3. ตรวจสอบข้อมูลจากฐานความรู้และระบุแหล่งอ้างอิงที่ควรมี
            4. สรุปเป็น 'ระดับความน่าเชื่อถือ' (เต็ม 100)
            """

            # เลือกใช้รุ่นที่เสถียรที่สุดสำหรับเว็บฟรี
            response = client.models.generate_content(
                model='gemini-1.5-flash', 
                contents=[prompt, uploaded_file]
            )
            
            status.update(label="✨ วิเคราะห์เสร็จสิ้น!", state="complete", expanded=False)

        # --- 4. แสดงผลลัพธ์สุดท้ายให้สวยงาม ---
        st.divider()
        st.subheader("📝 รายงานผลการตรวจสอบ")
        st.markdown(response.text)
        
        # ลบไฟล์ชั่วคราวทิ้งเพื่อประหยัดพื้นที่เซิร์ฟเวอร์
        if os.path.exists("temp_video.mp4"):
            os.remove("temp_video.mp4")
            
    else:
        st.warning("⚠️ กรุณาวางลิงก์วิดีโอก่อนครับ")
